## BERT INRODUCTION

This portion contains the Introduction to Bidirectional Encoder Representations from Transformers (BERT).

I tried to work with BERT for question answering task using finetuning on trained model (the results are not that good). But It introduces intial working steps for using BERT model.

Moreover minor analysis can be found in report.
